<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".home.HomeFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_private_camera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/BaseTextViewStyle.SubTitle"
            android:text="@string/private_camera"
            android:layout_marginHorizontal="@dimen/margin_10dp"
            android:layout_marginVertical="@dimen/margin_16dp"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_private_camera"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="horizontal" />

        <TextView
            android:id="@+id/tv_public_camera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/BaseTextViewStyle.SubTitle"
            android:text="@string/public_camera"
            android:layout_marginHorizontal="@dimen/margin_10dp"
            android:layout_marginVertical="@dimen/margin_16dp"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_public_camera"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="vertical" />

    </LinearLayout>

</ScrollView>